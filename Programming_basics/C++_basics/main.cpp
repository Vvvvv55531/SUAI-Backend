#include<iostream>
using namespace std;

int main() 
{	
	double a, b, c, d, e, f;
	a = 7.5 / (1.0 + 1.0 / 2.0);
	b = 2.0 * (3.0 / (7.0 + 1.0 / 2.0)) * (2.0 + 1.0 / 2.0);
	c = ((2.0 + 2.0 / 7.0) - (46.0 / 49.0)) / (44.0 / 147.0);
	d = 4.0 / (6.0 + 2.0 / 5.0);
	e = ((6.0 + 1.0 / 2.0) / 26.0) / (1.0 + 1.0 / 3.0);
	f = ((4.0 + 5.0 / 9.0) - 17.0 / 18.0) * 36.0 / 65.0;

	double result = (a + b + c) / (d + e - f);
	cout << result;

	return 0;
}